---
phase: 02-pair-management
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - shared-reveal/tests/e2e/fixtures/auth.ts
  - shared-reveal/tests/e2e/auth.spec.ts
  - shared-reveal/tests/unit/pair.test.ts
  - shared-reveal/tests/e2e/pair.spec.ts
autonomous: true
requirements: [PAIR-01, PAIR-02, PAIR-03, PAIR-04, PAIR-05, PAIR-06, SEC-05]

must_haves:
  truths:
    - "tests/unit/pair.test.ts exists with describe/it stubs for PAIR-01 through PAIR-06 and SEC-05 Zod validation"
    - "tests/e2e/pair.spec.ts exists with failing happy-path and rejection tests"
    - "tests/e2e/fixtures/auth.ts exports secondAuthenticatedPage fixture using TEST_UID_02"
    - "signInAsTestUser accepts optional uid parameter; defaults to TEST_UID_01"
    - "auth.spec.ts URL assertion accepts /pair-setup OR /home (forward-compatible with Phase 2 route guard)"
  artifacts:
    - path: shared-reveal/tests/unit/pair.test.ts
      provides: "Unit test stubs for invite code generation, Zod schema, expiry logic, SEC-05 validation conditions"
    - path: shared-reveal/tests/e2e/pair.spec.ts
      provides: "E2E test stubs for two-user happy path, already-used-code rejection, third-user rejection"
    - path: shared-reveal/tests/e2e/fixtures/auth.ts
      provides: "Extended auth fixture with parameterized signInAsTestUser and secondAuthenticatedPage"
  key_links:
    - from: shared-reveal/tests/e2e/pair.spec.ts
      to: shared-reveal/tests/e2e/fixtures/auth.ts
      via: "import { test, expect } from './fixtures/auth'"
      pattern: "secondAuthenticatedPage"
---

## Phase Goal

**As an** authenticated user without a pair, **I want to** connect with exactly one other person using a one-time invite code, **so that** we share a private space that no third person can join, no matter what.

_(Note: The ROADMAP Phase 2 Goal is not in user-story format. The above story is derived from the goal and locked decisions. If this phrasing needs adjustment, run `/gsd mvp-phase 2`.)_

<objective>
Create all test scaffolding — unit stubs, E2E stubs, and fixture extensions — that Wave 1 implementation plans depend on. Running tests before implementation guarantees the Nyquist rule: every behavior has a test that can go red then green.

Purpose: Establish the test harness so that each implementation plan in Waves 1–3 can verify its work with `npm test` and `npm run test:e2e` against pre-existing specs.

Output:
- `tests/unit/pair.test.ts` — Vitest stubs for PAIR-01 through PAIR-06 and SEC-05 validation logic
- `tests/e2e/pair.spec.ts` — Playwright stubs for the two-user happy path and rejection flows
- `tests/e2e/fixtures/auth.ts` — Extended with `secondAuthenticatedPage` and parameterized `signInAsTestUser`
- `tests/e2e/auth.spec.ts` — Updated URL assertion to handle /pair-setup AND /home (Phase 2 route guard compat)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-pair-management/02-CONTEXT.md
@.planning/phases/02-pair-management/02-RESEARCH.md
@.planning/phases/02-pair-management/02-VALIDATION.md
</context>

<interfaces>
<!-- Existing auth fixture — read before modifying -->
<!-- Source: shared-reveal/tests/e2e/fixtures/auth.ts -->

Current shape (read the file first — do not assume):
- TEST_UID = 'e2e-test-user-01'
- signInAsTestUser(page: Page): Promise<void> — no uid param, hardcoded TEST_UID
- waitForURL pattern: await page.waitForURL('**/home', { timeout: 10_000 })
- exports: test (base.extend with authenticatedPage fixture), expect

Target shape after this plan:
- TEST_UID_01 = 'e2e-test-user-01'  (rename TEST_UID → TEST_UID_01 for clarity)
- TEST_UID_02 = 'e2e-test-user-02'
- signInAsTestUser(page: Page, uid?: string): Promise<void>  (uid defaults to TEST_UID_01)
- waitForURL pattern: await page.waitForURL(/\/(pair-setup|home)/, { timeout: 10_000 })
- exports: test (base.extend with authenticatedPage + secondAuthenticatedPage), expect

Existing auth.spec.ts assertion to update:
  await expect(page).toHaveURL(/\/home/)
  → await expect(page).toHaveURL(/\/(pair-setup|home)/)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend auth fixture and update auth.spec.ts for Phase 2 route guard compat</name>
  <files>
    shared-reveal/tests/e2e/fixtures/auth.ts
    shared-reveal/tests/e2e/auth.spec.ts
  </files>
  <read_first>
    - shared-reveal/tests/e2e/fixtures/auth.ts (full file — understand current shape before changing)
    - shared-reveal/tests/e2e/auth.spec.ts (full file — see current URL assertion)
  </read_first>
  <action>
    Refactor auth.ts: rename TEST_UID to TEST_UID_01 (keep value 'e2e-test-user-01'); add TEST_UID_02 = 'e2e-test-user-02'. Add uid parameter to signInAsTestUser with default TEST_UID_01 — replace the hardcoded TEST_UID reference inside the function with the uid parameter.

    Change the waitForURL call from 'test-*/home*' to the regex /\/(pair-setup|home)/ with timeout 10_000. After Phase 2's App.tsx route guard lands, fresh test users (no pairId) redirect to /pair-setup; this fixture must accept both destinations.

    Add secondAuthenticatedPage to the base.extend type signature: { authenticatedPage: Page; secondAuthenticatedPage: Page }. Implement it using browser.newPage() (new browser context, not the shared page): create a new page, call signInAsTestUser(page, TEST_UID_02), use it, then close it in cleanup. This isolates the two users in separate browser sessions so neither shares cookies or IndexedDB state.

    In auth.spec.ts: update the toHaveURL assertion from /\/home/ to /\/(pair-setup|home)/. Update the test name to 'authenticated fixture lands on an authenticated screen' (the current name 'lands on /home' will be misleading after Phase 2). Remove the page.getByText('Bird Eye') assertion — that text lives on /home and is not guaranteed after the route guard is added (the user may now land on /pair-setup).
  </action>
  <verify>
    <automated>cd shared-reveal && npm run test:e2e -- --grep "authenticated fixture" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - auth.ts exports `test` with fixture type `{ authenticatedPage: Page; secondAuthenticatedPage: Page }`
    - `signInAsTestUser` function signature is `async function signInAsTestUser(page: Page, uid = TEST_UID_01): Promise<void>`
    - `TEST_UID_02 = 'e2e-test-user-02'` is defined at module scope
    - auth.ts `waitForURL` call matches regex `/\/(pair-setup|home)/` not the string `'**/home'`
    - auth.spec.ts test passes (exit 0) against the emulator with the updated URL assertion
    - auth.spec.ts no longer asserts `page.getByText('Bird Eye')`
  </acceptance_criteria>
  <done>auth fixture supports two authenticated users; auth.spec.ts test passes; URL assertion is forward-compatible with Phase 2 route guard</done>
</task>

<task type="auto">
  <name>Task 2: Create unit test stubs for pair validation logic</name>
  <files>
    shared-reveal/tests/unit/pair.test.ts
  </files>
  <read_first>
    - shared-reveal/src/types/index.ts (existing UserDoc shape — PairDoc will extend this file)
    - .planning/phases/02-pair-management/02-RESEARCH.md (Pattern 2: JoinPairSchema; Code Examples: Zod schema, invite code generation)
    - .planning/phases/02-pair-management/02-VALIDATION.md (Per-Task Verification Map — all stubs to create)
  </read_first>
  <action>
    Create shared-reveal/tests/unit/pair.test.ts (note: the tests/unit/ directory does not exist yet — create it). This is the first unit test file in the codebase; use Vitest import pattern: `import { describe, it, expect } from 'vitest'`.

    Write the following describe/it blocks using it.todo() stubs for behaviors not yet implemented, and concrete it() tests for pure-logic that can already be verified:

    describe('invite code generation') — one concrete test: import randomBytes from 'node:crypto' and verify `randomBytes(3).toString('hex').toUpperCase()` matches /^[A-F0-9]{6}$/ (PAIR-01, assumption A1).

    describe('JoinPairSchema') — import the Zod schema from a path where it will live after Wave 1 implementation. For now, define the schema inline in the test (to make the test runnable immediately) using `import { z } from 'zod'` and the pattern: `z.object({ inviteCode: z.string().length(6).regex(/^[A-F0-9]{6}$/) })`. Write three concrete tests: accepts 'A1B2C3' (success), rejects 'abc' (lowercase, fail), rejects 'TOOLONG7' (length, fail). These will be replaced by an import from functions once Wave 1 ships (PAIR-01, SEC-05).

    describe('invite code expiry') — two it.todo stubs: 'rejects invite code older than 24 hours' (PAIR-02), 'accepts invite code created within 24 hours' (PAIR-02).

    describe('joinPair validation conditions — SEC-05') — five it.todo stubs: 'rejects expired code', 'rejects already-used code' (PAIR-05), 'rejects when pair already has 2 members' (PAIR-04), 'rejects when requester is the creator', 'rejects when requester already has pairId' (PAIR-06).

    describe('createPair — PAIR-06') — one it.todo stub: 'rejects when creator already has a pairId'.

    No Firebase Admin SDK imports — these tests run in Node/Vitest without emulator.
  </action>
  <verify>
    <automated>cd shared-reveal && npx vitest run tests/unit/pair.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `shared-reveal/tests/unit/pair.test.ts`
    - `npx vitest run tests/unit/pair.test.ts` exits 0 with no failures (stubs are it.todo, concrete tests pass)
    - The invite code generation test asserts `.toMatch(/^[A-F0-9]{6}$/)` and passes
    - JoinPairSchema tests for 'A1B2C3' passes, 'abc' fails (returns success:false), 'TOOLONG7' fails
    - No Firebase Admin SDK or firebase-functions imports in this file
    - File has at minimum: 3 describe blocks, at least 3 concrete it() tests, at least 5 it.todo() stubs
  </acceptance_criteria>
  <done>Unit test stubs created; concrete schema and code-generation tests pass; todo stubs mark all Wave 1 behaviors</done>
</task>

<task type="auto">
  <name>Task 3: Create E2E test stubs for pair happy path and rejection flows</name>
  <files>
    shared-reveal/tests/e2e/pair.spec.ts
  </files>
  <read_first>
    - shared-reveal/tests/e2e/fixtures/auth.ts (post-Task-1 shape — verify secondAuthenticatedPage is exported)
    - shared-reveal/tests/e2e/auth.spec.ts (import pattern to replicate)
    - .planning/phases/02-pair-management/02-PATTERNS.md (pair.spec.ts section: test patterns and waitForURL pattern)
  </read_first>
  <action>
    Create shared-reveal/tests/e2e/pair.spec.ts. Import from './fixtures/auth' (both test and expect).

    Write three test stubs:

    test.fail('User A creates pair and receives 6-char invite code', async ({ authenticatedPage: page }) => { ... }) — stub body: wait for /pair-setup URL (timeout 10_000), click the "Create a pair" button, assert a text matching /[A-F0-9]{6}/ is visible. Mark with test.fail() because the implementation does not exist yet (PAIR-01).

    test.fail('User B joins pair and both users redirect to /home', async ({ authenticatedPage: pageA, secondAuthenticatedPage: pageB }) => { ... }) — stub body: pageA creates pair and copies the invite code text; pageB fills the code input and triggers auto-submit; assert both pageA and pageB land on /home. Mark with test.fail() (PAIR-03, PAIR-05).

    test.fail('Third user is rejected when pair is full', async ({ authenticatedPage: pageA, secondAuthenticatedPage: pageB }) => { ... }) — stub body: A creates pair, B joins, a third user (use a fresh browser context with TEST_UID_03 = 'e2e-test-user-03' inline) attempts the same code and receives an error message. Mark with test.fail() (PAIR-04).

    Use test.fail() not test.skip() so that when these tests start passing unexpectedly, the runner flags them for review. Once Wave 3 is complete, remove test.fail() wrappers.

    Add at the top of the file: `// E2E stubs — marked test.fail() until Wave 3 (pair UI) is complete`
  </action>
  <verify>
    <automated>cd shared-reveal && npm run test:e2e -- tests/e2e/pair.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `shared-reveal/tests/e2e/pair.spec.ts`
    - `npm run test:e2e -- tests/e2e/pair.spec.ts` exits 0 (test.fail() tests pass the runner when they fail as expected)
    - File imports `{ test, expect }` from `'./fixtures/auth'` (not from @playwright/test directly)
    - Three test blocks exist: create-pair, join-pair, third-user-rejection
    - All three use `test.fail()` wrapper
    - `secondAuthenticatedPage` is used as a fixture in at least two tests
  </acceptance_criteria>
  <done>E2E stubs exist and runner exits 0; test.fail() stubs will flip to normal tests when Wave 3 implementation lands</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → emulator | Test fixture calls Auth emulator with Admin SDK custom token — no real user credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00-01 | Information Disclosure | TEST_UID_01 / TEST_UID_02 in fixture | accept | Values are fake UIDs used only in emulator; never in production data |
| T-02-00-02 | Elevation of Privilege | secondAuthenticatedPage uses browser.newPage() | mitigate | New page creates isolated browser context with no shared cookies/IndexedDB — prevents session bleed between test users |
</threat_model>

<verification>
After all three tasks complete:

1. Run `cd shared-reveal && npm run test:e2e -- tests/e2e/auth.spec.ts` — must exit 0 (existing auth test still passes with updated URL assertion).
2. Run `cd shared-reveal && npx vitest run tests/unit/pair.test.ts` — must exit 0 with concrete tests passing and stubs listed as todo.
3. Run `cd shared-reveal && npm run test:e2e -- tests/e2e/pair.spec.ts` — must exit 0 (test.fail() stubs are expected failures).
4. Verify `secondAuthenticatedPage` is in the exported fixture type by running `npx tsc --noEmit` in shared-reveal/ with exit 0.
</verification>

<success_criteria>
- `tests/unit/pair.test.ts` exists with 5+ it.todo() stubs and 3+ passing concrete tests (exit 0)
- `tests/e2e/pair.spec.ts` exists with 3 test.fail() stubs (exit 0)
- `tests/e2e/fixtures/auth.ts` exports secondAuthenticatedPage; signInAsTestUser takes optional uid
- `tests/e2e/auth.spec.ts` URL assertion accepts /pair-setup OR /home
- `npm run test:e2e` full suite exits 0
- `npx tsc --noEmit` in shared-reveal/ exits 0
</success_criteria>

<output>
Create `.planning/phases/02-pair-management/02-00-SUMMARY.md` when done.
</output>
