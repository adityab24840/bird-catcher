---
id: "01-01"
wave: 1
depends_on: []
phase: 1
phase_name: "Foundation"
type: execute
objective: "Scaffold the shared-reveal/ project root and configure the full build toolchain — Vite 8 + React 18/19 + TypeScript 5.5+ + Tailwind v4 (Vite plugin) + vite-plugin-pwa (injectManifest) + the unified service worker skeleton (Workbox precache + Firebase Storage exclusion) + the PWA manifest + Firebase project config files — so that `npm run dev` and `npm run build` both succeed."
requirements_addressed: [PWA-02, PWA-04, PWA-05]
files_modified:
  - shared-reveal/package.json
  - shared-reveal/tsconfig.json
  - shared-reveal/tsconfig.app.json
  - shared-reveal/tsconfig.node.json
  - shared-reveal/vite.config.ts
  - shared-reveal/.gitignore
  - shared-reveal/.env.example
  - shared-reveal/index.html
  - shared-reveal/src/main.tsx
  - shared-reveal/src/App.tsx
  - shared-reveal/src/styles/global.css
  - shared-reveal/src/sw.ts
  - shared-reveal/src/vite-env.d.ts
  - shared-reveal/public/icons/icon-192.png
  - shared-reveal/public/icons/icon-512.png
  - shared-reveal/public/icons/icon-512-maskable.png
  - shared-reveal/public/icons/apple-touch-icon-180.png
  - shared-reveal/firebase.json
  - shared-reveal/.firebaserc
  - shared-reveal/firestore.rules
  - shared-reveal/storage.rules
autonomous: false
scaffolding_exception: true  # Phase 1 bootstrap — 20 files are scaffolding/config templates, not feature authoring. Each file is trivially small. Split into 3a/3b reduces per-task scope.

must_haves:
  truths:
    - "`npm install` completes without peer-dependency errors in shared-reveal/"
    - "`npm run dev` starts the Vite dev server without errors"
    - "`npm run build` produces a valid production bundle in dist/"
    - "The PWA manifest is emitted with name, short_name, icons (192/512/maskable), theme_color, background_color, display: standalone, start_url"
    - "The service worker excludes firebasestorage.googleapis.com from fetch interception"
  artifacts:
    - path: "shared-reveal/vite.config.ts"
      provides: "Vite config: React + Tailwind v4 plugin + vite-plugin-pwa injectManifest + define block for SW Firebase config"
      contains: "strategies: 'injectManifest'"
    - path: "shared-reveal/src/sw.ts"
      provides: "Unified service worker skeleton: Workbox precache + Firebase Storage fetch exclusion"
      contains: "firebasestorage.googleapis.com"
    - path: "shared-reveal/src/styles/global.css"
      provides: "Tailwind v4 CSS-first entry with @theme"
      contains: "@import \"tailwindcss\""
    - path: "shared-reveal/firebase.json"
      provides: "Firebase project config: hosting, functions, firestore/storage rules, emulator ports"
  key_links:
    - from: "shared-reveal/vite.config.ts"
      to: "shared-reveal/src/sw.ts"
      via: "vite-plugin-pwa injectManifest srcDir/filename"
      pattern: "filename: 'sw.ts'"
---

<objective>
Scaffold the `shared-reveal/` project root and configure every build tool the rest of the project depends on. This is the Walking Skeleton's foundation task: after this plan, the project builds, the dev server runs, the PWA manifest is valid, and the unified service worker skeleton is in place (with the Firebase Storage exclusion that Phase 3 uploads depend on).

Purpose: Every subsequent plan and phase builds on this toolchain. Getting the Tailwind v4, vite-plugin-pwa injectManifest, and Vite `define` decisions right now avoids a rewrite later (per RESEARCH.md pitfalls 2, 3).

Output: A buildable, runnable `shared-reveal/` project with PWA manifest + SW skeleton + Firebase project config files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-SKELETON.md
@.planning/ROADMAP.md
@CLAUDE.md

<constraints>
- Tailwind v4: use `@tailwindcss/vite` ONLY. Do NOT install `@tailwindcss/postcss`. Do NOT create `tailwind.config.ts` or `postcss.config.js`. Config lives in CSS via `@theme` (RESEARCH.md Pattern 1, Anti-Patterns).
- vite-plugin-pwa: `strategies: 'injectManifest'` ONLY (NOT `generateSW`), `srcDir: 'src'`, `filename: 'sw.ts'` (RESEARCH.md Pattern 1, Pitfall 3).
- `import.meta.env` is undefined in the service worker. Firebase config for the SW must be injected via the Vite `define` block as `__FIREBASE_*__` literal identifiers (RESEARCH.md Pattern 1, Pitfall 2).
- Vite 8.x (verified compatible with vite-plugin-pwa@1.3.0 peer deps). Firebase SDK v12 modular imports only.
- Node 22 runtime for Functions (firebase.json); local Node is 25.9.0, npm 11.12.1, Firebase CLI 14.11.0 (all present per RESEARCH.md Environment Availability).
</constraints>

<versions>
From RESEARCH.md Standard Stack (verified on npm registry):
- react@19, react-dom@19, react-router-dom@7, firebase@12.18.0, zod@4.5.4
- vite@8.2.2, @vitejs/plugin-react (latest), typescript@5.5+, tailwindcss@4.3.3, @tailwindcss/vite@4.3.3
- vite-plugin-pwa@1.3.0, workbox-precaching@7.4.1, workbox-routing@7.4.1
- vitest@4.1.11, @testing-library/react (latest), @testing-library/user-event, @testing-library/jest-dom, jsdom
- @playwright/test@1.62.1, firebase-admin@14.3.0, @firebase/rules-unit-testing@5.0.2
</versions>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification (supply-chain gate)</name>
  <what-built>Nothing yet — this gate runs BEFORE any `npm install`. RESEARCH.md's Package Legitimacy Audit ran without slopcheck available, so every package is marked [ASSUMED]. The audit dispositions all packages as Approved (multi-year history; Google/Meta/Microsoft/Vercel/Tailwind-Labs maintained), but the supply-chain gate requires human confirmation before install.</what-built>
  <how-to-verify>
    Confirm these package names resolve to the expected first-party maintainers on npmjs.com (spot-check the non-obvious ones):
    1. https://www.npmjs.com/package/vite-plugin-pwa  (maintainer: vite-pwa org)
    2. https://www.npmjs.com/package/@tailwindcss/vite  (maintainer: Tailwind Labs)
    3. https://www.npmjs.com/package/@firebase/rules-unit-testing  (maintainer: Google/Firebase)
    4. https://www.npmjs.com/package/workbox-precaching and workbox-routing  (maintainer: Google)
    5. Confirm firebase, firebase-admin, react, react-dom, vite, @vitejs/plugin-react, typescript, tailwindcss, zod, react-router-dom, vitest, @testing-library/*, @playwright/test are the expected canonical packages (no typosquats).
    Reject if any package name differs from the list in <versions> or resolves to an unknown maintainer.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to install, or list any package that fails verification.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create project structure, package.json, TS config, and install dependencies</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack, Installation, Recommended Project Structure)
    - .planning/phases/01-foundation/01-SKELETON.md (Directory layout, Stack Touched)
  </read_first>
  <files>
    shared-reveal/package.json, shared-reveal/tsconfig.json, shared-reveal/tsconfig.app.json,
    shared-reveal/tsconfig.node.json, shared-reveal/.gitignore, shared-reveal/.env.example,
    shared-reveal/src/vite-env.d.ts
  </files>
  <action>
    Create the `shared-reveal/` project root and the directory tree from the phase brief: src/{components,pages,layouts,hooks,services,firebase,types,utils,styles}, public/icons, functions/src, tests/e2e.
    Author package.json with: name "shared-reveal", type "module", and scripts `dev` (vite), `build` (`tsc -b && vite build`), `preview` (vite preview), `test` (vitest run), `test:e2e` (playwright test), `lint` (tsc --noEmit). Add the exact dependencies and devDependencies from the <versions> block (app deps: react, react-dom, react-router-dom, firebase, zod; dev deps: vite, @vitejs/plugin-react, typescript, tailwindcss, @tailwindcss/vite, vite-plugin-pwa, workbox-precaching, workbox-routing, vitest, @testing-library/react, @testing-library/user-event, @testing-library/jest-dom, jsdom, @playwright/test, firebase-admin, @firebase/rules-unit-testing).
    Create tsconfig.json as a project-references root pointing to tsconfig.app.json (src, DOM + WebWorker libs, target ES2022, "moduleResolution": "bundler", "strict": true, "types" including vite-plugin-pwa/client) and tsconfig.node.json (vite.config.ts). Ensure `"lib"` in tsconfig.app.json includes "WebWorker" so src/sw.ts type-checks.
    Add src/vite-env.d.ts referencing `/// <reference types="vite/client" />`, `/// <reference types="vite-plugin-pwa/client" />`, and declaring the six `__FIREBASE_*__` const identifiers as `string` (so sw.ts compiles against the Vite `define` injection).
    Create .gitignore (node_modules, dist, .env, .env.local, .env.test, firebase-debug.log, functions/lib, functions/node_modules, coverage, playwright-report, test-results, *.local, service-account.json).
    Create .env.example listing VITE_FIREBASE_API_KEY, VITE_FIREBASE_AUTH_DOMAIN, VITE_FIREBASE_PROJECT_ID, VITE_FIREBASE_STORAGE_BUCKET, VITE_FIREBASE_MESSAGING_SENDER_ID, VITE_FIREBASE_APP_ID, VITE_FIREBASE_AUTH_EMULATOR_HOST, VITE_FIRESTORE_EMULATOR_HOST with placeholder values and inline comments (note that VITE_FIREBASE_AUTH_DOMAIN MUST be the custom Firebase Hosting domain, not *.firebaseapp.com — SEC-08).
    Run `npm install` in shared-reveal/. Resolve any peer-dependency error by pinning to the versions in <versions>; do NOT downgrade Vite below 8 or TypeScript below 5.5.
  </action>
  <acceptance_criteria>
    - Directory `shared-reveal/` exists with all subfolders listed above.
    - `npm install` in shared-reveal/ exits 0 with no peer-dependency ERESOLVE errors.
    - package.json contains no reference to `@tailwindcss/postcss`.
    - .env.example contains all eight VITE_* keys.
    - No `tailwind.config.ts` and no `postcss.config.js` exist anywhere in shared-reveal/.
  </acceptance_criteria>
  <verify>
    <automated>cd shared-reveal && npm install && test ! -f tailwind.config.ts && test ! -f postcss.config.js && ! grep -q "@tailwindcss/postcss" package.json && echo OK</automated>
  </verify>
  <done>Dependencies install cleanly; project tree and TS config exist; no Tailwind PostCSS artifacts present.</done>
</task>

<task type="auto">
  <name>Task 3a: Configure Vite + Tailwind v4 + PWA + service worker skeleton + app shell files</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1 Vite config, Pattern 2 service worker)
    - shared-reveal/package.json (from Task 2)
  </read_first>
  <files>
    shared-reveal/vite.config.ts, shared-reveal/src/styles/global.css, shared-reveal/src/sw.ts,
    shared-reveal/index.html, shared-reveal/src/main.tsx, shared-reveal/src/App.tsx,
    shared-reveal/src/pages/LandingPage.tsx, shared-reveal/src/pages/HomePage.tsx,
    shared-reveal/public/icons/icon-192.png, shared-reveal/public/icons/icon-512.png,
    shared-reveal/public/icons/icon-512-maskable.png, shared-reveal/public/icons/apple-touch-icon-180.png
  </files>
  <action>
    Author vite.config.ts per RESEARCH.md Pattern 1: `plugins: [react(), tailwindcss(), VitePWA({...})]`. The VitePWA config MUST set `registerType: 'autoUpdate'`, `strategies: 'injectManifest'`, `srcDir: 'src'`, `filename: 'sw.ts'`, `devOptions: { enabled: false }`, and a `manifest` object satisfying PWA-02 (name "Reveal", short_name "Reveal", description, theme_color "#ffffff", background_color "#ffffff", display "standalone", start_url "/", and the three icons entries pointing to /icons/icon-192.png, /icons/icon-512.png, and /icons/icon-512-maskable.png with purpose "maskable"). Add the `define` block mapping the six `__FIREBASE_*__` identifiers to `JSON.stringify(process.env.VITE_FIREBASE_*)` so the SW receives config at build time (PWA-04/Pitfall 2).
    Author src/sw.ts as the unified service worker SKELETON per Pattern 2: declare `self` as ServiceWorkerGlobalScope, call `precacheAndRoute(self.__WB_MANIFEST)` and `cleanupOutdatedCaches()` from workbox-precaching, and register a `fetch` event listener that early-returns (passes through, does NOT intercept/cache) any request whose URL hostname is `firebasestorage.googleapis.com` (PWA-05/Pitfall 6). Do NOT add the FCM `onBackgroundMessage` handler in this plan — that is added in plan 01-03 to complete the unified SW. Add a code comment marking the FCM insertion point.
    Author src/styles/global.css with `@import "tailwindcss";` and a minimal `@theme { }` block (a couple of tokens — Claude's discretion per RESEARCH.md).
    Author index.html with `<link rel="apple-touch-icon" href="/icons/apple-touch-icon-180.png">`, theme-color meta, viewport meta, `#root`, and the `src/main.tsx` module script.
    Author src/main.tsx (React root, imports global.css, wraps App in BrowserRouter, calls the vite-plugin-pwa `registerSW({ immediate: true })` from `virtual:pwa-register`) and a minimal src/App.tsx with routes at `/` and `/home`. Author placeholder src/pages/LandingPage.tsx and src/pages/HomePage.tsx stubs (real implementations added in plan 01-02). Icon art is Claude's discretion per RESEARCH.md.
    Generate the four PWA icons in public/icons at the required sizes (192, 512, 512 maskable, 180 apple-touch) — solid-color placeholder PNGs at exact pixel dimensions are acceptable for the skeleton.
  </action>
  <acceptance_criteria>
    - src/sw.ts contains a fetch handler that early-returns for `firebasestorage.googleapis.com`.
    - vite.config.ts contains `strategies: 'injectManifest'` and a `define` block with all six `__FIREBASE_*__` keys.
    - index.html contains the apple-touch-icon link.
    - All four icon files exist in public/icons/ at the correct pixel dimensions.
    - Placeholder page stubs exist at src/pages/LandingPage.tsx and src/pages/HomePage.tsx.
  </acceptance_criteria>
  <verify>
    <automated>cd shared-reveal && grep -q "injectManifest" vite.config.ts && grep -q "firebasestorage.googleapis.com" src/sw.ts && test -f public/icons/icon-192.png && test -f public/icons/icon-512.png && test -f src/pages/LandingPage.tsx && test -f src/pages/HomePage.tsx && echo OK</automated>
  </verify>
  <done>App shell skeleton files and Vite/PWA/SW configuration in place; Storage exclusion wired.</done>
</task>

<task type="auto">
  <name>Task 3b: Firebase project config files + prove the build</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 6 Firebase config files)
    - shared-reveal/vite.config.ts (from Task 3a, to confirm build entry)
  </read_first>
  <files>
    shared-reveal/firebase.json, shared-reveal/.firebaserc,
    shared-reveal/firestore.rules, shared-reveal/storage.rules
  </files>
  <action>
    Author the Firebase project config files per Pattern 6: firebase.json (firestore.rules, storage.rules, functions source "functions" runtime nodejs22, hosting public "dist" with SPA rewrite to /index.html, emulators for auth:9099 firestore:8080 storage:9199 functions:5001 ui:4000); .firebaserc with a `default` project alias placeholder `<your-firebase-project-id>`; firestore.rules with the /users/{uid} own-read/own-create/own-update rules that block client writes to pairId (`!request.resource.data.diff(resource.data).affectedKeys().hasAny(['pairId'])`) and a catch-all deny; storage.rules deny-all.
    Run `npm run build` and `npm run dev` (start, confirm it serves, stop) to prove the full toolchain end-to-end.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0 and produces shared-reveal/dist/ containing index.html, a hashed JS bundle, the generated manifest (manifest.webmanifest or equivalent), and the compiled sw.js.
    - The emitted manifest contains: name, short_name, icons array with 192 and 512 entries, theme_color, background_color, `"display":"standalone"`, `start_url`.
    - firestore.rules contains a `match /users/{uid}` block and denies client writes to `pairId`.
    - `npm run dev` serves the app on its dev port and the root route renders without a console error.
  </acceptance_criteria>
  <verify>
    <automated>cd shared-reveal && npm run build && test -d dist && grep -q "match /users/{uid}" firestore.rules && node -e "const fs=require('fs');const m=fs.readdirSync('dist').find(x=>x.includes('manifest'));if(!m)process.exit(1);const j=JSON.parse(fs.readFileSync('dist/'+m));if(j.display!=='standalone'||!j.icons.some(i=>i.sizes==='512x512'))process.exit(1);console.log('manifest OK')" && echo OK</automated>
  </verify>
  <done>Firebase project config files written; production build succeeds; manifest and unified-SW skeleton emitted; dev server runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local build | Third-party package code executes during install/build |
| build output → browser SW scope | Injected Firebase config literals ship to the client SW |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (all [ASSUMED] packages) | mitigate | Blocking human package-legitimacy checkpoint (Task 1) before any install; verify canonical maintainers on npmjs.com |
| T-01-01 | Tampering | Malicious/rogue service worker registration | mitigate | vite-plugin-pwa scoped to own origin; sw.ts only precaches own build assets; no third-party fetch caching |
| T-01-02 | Information Disclosure | Firebase web config in client bundle (API key, authDomain) | accept | Firebase web API keys are not secrets (public by design); real protection is Firestore/Storage rules + App Check (Phase 2). No Admin credentials in client. |
</threat_model>

<verification>
- `cd shared-reveal && npm install` → exits 0.
- `cd shared-reveal && npm run dev` → serves without error.
- `cd shared-reveal && npm run build` → dist/ produced with valid manifest and compiled SW.
- No tailwind.config.ts / postcss.config.js; no @tailwindcss/postcss dependency.
</verification>

<success_criteria>
- `npm install` then `npm run dev` works; `npm run build` produces a valid production bundle (Walking Skeleton must-haves 1 and last).
- PWA manifest satisfies PWA-02; injectManifest + SW skeleton satisfies PWA-04; Storage exclusion satisfies PWA-05.
- Firebase project config files and /users firestore rules are in place for the auth wave.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done.
</output>
