---
id: "01-03"
wave: 3
depends_on: ["01-02"]
phase: 1
phase_name: "Foundation"
type: execute
objective: "Complete the PWA slice: finish the unified service worker by adding the FCM onBackgroundMessage handler (so Workbox precache + FCM coexist in one SW — PWA-04), add an offline-detection hook and a graceful 'You're offline' shell (PWA-03), add the iOS install-education banner, and verify the app is installable on desktop Chrome with the SW registering exactly once (PWA-01)."
requirements_addressed: [PWA-01, PWA-03, PWA-04]
files_modified:
  - shared-reveal/src/sw.ts
  - shared-reveal/src/hooks/useOnline.ts
  - shared-reveal/src/components/OfflineBanner.tsx
  - shared-reveal/src/components/IOSInstallBanner.tsx
  - shared-reveal/src/App.tsx
autonomous: false

must_haves:
  truths:
    - "The unified service worker handles both Workbox precaching and FCM background messages (FCM slot wired, no dual SW)"
    - "The app loads from the installed icon with no network and shows a 'You're offline' state, not a blank screen"
    - "The service worker registers exactly once — no infinite reload loop"
    - "iOS users who have not installed the app see an Add-to-Home-Screen guide; installed/standalone and non-iOS users do not"
    - "The app is installable on desktop Chrome (valid manifest + registered SW)"
  artifacts:
    - path: "shared-reveal/src/sw.ts"
      provides: "Unified SW: Workbox precache + Storage exclusion (from 01-01) + FCM onBackgroundMessage"
      contains: "onBackgroundMessage"
    - path: "shared-reveal/src/components/OfflineBanner.tsx"
      provides: "Graceful offline UI"
      contains: "offline"
    - path: "shared-reveal/src/components/IOSInstallBanner.tsx"
      provides: "iOS install education (isIOS && !isStandalone)"
      contains: "standalone"
    - path: "shared-reveal/src/hooks/useOnline.ts"
      provides: "navigator.onLine reactive state"
      contains: "onLine"
  key_links:
    - from: "shared-reveal/src/sw.ts"
      to: "firebase/messaging/sw"
      via: "getMessaging + onBackgroundMessage"
      pattern: "onBackgroundMessage"
    - from: "shared-reveal/src/App.tsx"
      to: "OfflineBanner + IOSInstallBanner"
      via: "conditional render"
      pattern: "OfflineBanner|IOSInstallBanner"
---

<objective>
Turn the buildable skeleton into a real installable, offline-capable PWA and finalize the unified service worker so it will never need a structural rewrite when FCM sends go live in Phase 5.

Purpose: The offline shell and single-SW guarantee are table-stakes (SUMMARY.md) and day-zero architecture (RESEARCH.md Pitfall 3). Wiring the FCM `onBackgroundMessage` slot now — even though no notifications are sent until Phase 5 — avoids the dual-SW infinite reload loop that a later retrofit would risk.

Output: A PWA that installs on Chrome, renders a graceful offline shell, shows the iOS install guide when appropriate, and runs one unified SW.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-SKELETON.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- sw.ts already exists from plan 01-01 with precacheAndRoute + Storage exclusion + a marked FCM insertion point. -->
<!-- App.tsx already renders routes (from 01-02); this plan mounts the two banners above the router outlet. -->

src/hooks/useOnline.ts exports:
  useOnline(): boolean   — reactive navigator.onLine, updated on 'online'/'offline' events

Firebase SW-side config (__FIREBASE_*__ literals) is injected by the Vite define block from plan 01-01.
</interfaces>

<constraints>
- Do NOT change the vite-plugin-pwa strategy — it stays `injectManifest` (PWA-04). Do NOT add a second service worker file (no firebase-messaging-sw.js) — the dual-SW reload loop is the exact failure to avoid (RESEARCH.md Pitfall 3).
- In src/sw.ts, initialize a NAMED Firebase app instance (e.g. '[SW]') using the `__FIREBASE_*__` define literals — NOT `import.meta.env` (undefined in SW scope, Pitfall 2). Import getMessaging/onBackgroundMessage from `firebase/messaging/sw`.
- Preserve the existing Storage exclusion fetch handler from plan 01-01 (PWA-05) — do not remove or override it.
- Phase 1 sends no FCM messages. The onBackgroundMessage handler is registered but only shows a notification if a payload arrives; this is the ready slot for Phase 5.
- iOS detection: `isIOS` = userAgent matches iphone/ipad/ipod; `isStandalone` = `navigator.standalone === true`. Banner shows only when `isIOS && !isStandalone` (RESEARCH.md Pattern 7). This is the v1 iOS install-education artifact called out in SUMMARY.md Spec Gap #3.
- Offline shell must render meaningful UI (app chrome + "You're offline" message), never a blank white screen (RESEARCH.md Pattern; iOS 7-day cache note in CLAUDE.md STACK).
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the unified service worker (add FCM onBackgroundMessage slot)</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2 unified SW, Pitfall 2 and 3)
    - shared-reveal/src/sw.ts (skeleton from plan 01-01 with precache + Storage exclusion + FCM insertion marker)
    - shared-reveal/vite.config.ts (define block providing __FIREBASE_*__)
  </read_first>
  <files>shared-reveal/src/sw.ts</files>
  <action>
    Extend src/sw.ts at the marked FCM insertion point per RESEARCH.md Pattern 2 WITHOUT altering the existing precacheAndRoute/cleanupOutdatedCaches calls or the firebasestorage.googleapis.com fetch exclusion. Build a `firebaseConfig` object from the `__FIREBASE_API_KEY__`, `__FIREBASE_AUTH_DOMAIN__`, `__FIREBASE_PROJECT_ID__`, `__FIREBASE_STORAGE_BUCKET__`, `__FIREBASE_MESSAGING_SENDER_ID__`, `__FIREBASE_APP_ID__` define literals. Call `initializeApp(firebaseConfig, '[SW]')` (named instance to avoid collision), `getMessaging(app)` from `firebase/messaging/sw`, and register `onBackgroundMessage(messaging, payload => self.registration.showNotification(payload.notification?.title ?? 'Reveal', { body: payload.notification?.body, icon: '/icons/icon-192.png' }))`. Keep the handler defensive (optional chaining) since Phase 1 sends no messages.
    Confirm `npm run build` compiles sw.ts (the WebWorker lib + vite-plugin-pwa/client types from plan 01-01 must cover `self.__WB_MANIFEST`).
  </action>
  <acceptance_criteria>
    - src/sw.ts still contains the precacheAndRoute call and the firebasestorage.googleapis.com exclusion (unchanged).
    - src/sw.ts now imports from `firebase/messaging/sw` and registers `onBackgroundMessage`.
    - src/sw.ts references the `__FIREBASE_*__` define literals, not `import.meta.env`.
    - `npm run build` compiles the SW and emits it into dist/.
    - No second service worker file exists (no firebase-messaging-sw.js).
  </acceptance_criteria>
  <verify>
    <automated>cd shared-reveal && grep -q "onBackgroundMessage" src/sw.ts && grep -q "firebasestorage.googleapis.com" src/sw.ts && grep -q "__FIREBASE_API_KEY__" src/sw.ts && ! grep -q "import.meta.env" src/sw.ts && test ! -f public/firebase-messaging-sw.js && test ! -f src/firebase-messaging-sw.js && npm run build && echo OK</automated>
  </verify>
  <done>One unified SW handles Workbox precache + Storage exclusion + FCM background messages (PWA-04).</done>
</task>

<task type="auto">
  <name>Task 2: Offline detection hook, offline shell, and iOS install banner</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 7 iOS install banner)
    - shared-reveal/src/App.tsx (routes from plan 01-02)
  </read_first>
  <files>
    shared-reveal/src/hooks/useOnline.ts, shared-reveal/src/components/OfflineBanner.tsx,
    shared-reveal/src/components/IOSInstallBanner.tsx, shared-reveal/src/App.tsx
  </files>
  <action>
    Author src/hooks/useOnline.ts: return reactive `navigator.onLine`, subscribing to window 'online'/'offline' events and cleaning up on unmount.
    Author src/components/OfflineBanner.tsx: when `useOnline()` is false, render a clear, styled "You're offline" state (persistent banner or full-shell message) that keeps app chrome visible — never a blank screen (PWA-03).
    Author src/components/IOSInstallBanner.tsx per RESEARCH.md Pattern 7: compute `isIOS` (userAgent iphone/ipad/ipod) and `isStandalone` (`navigator.standalone === true`); return null unless `isIOS && !isStandalone`; otherwise render the "tap Share → Add to Home Screen" guidance.
    Edit src/App.tsx to mount `<OfflineBanner />` and `<IOSInstallBanner />` around the router outlet so they appear on every route without interfering with auth routing from plan 01-02.
  </action>
  <acceptance_criteria>
    - useOnline subscribes to 'online' and 'offline' events and returns a boolean.
    - OfflineBanner renders offline UI only when offline and preserves app chrome.
    - IOSInstallBanner returns null when not iOS or when standalone; renders guidance only when `isIOS && !isStandalone`.
    - App.tsx renders both components; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd shared-reveal && grep -q "onLine" src/hooks/useOnline.ts && grep -qi "offline" src/components/OfflineBanner.tsx && grep -q "standalone" src/components/IOSInstallBanner.tsx && grep -q "IOSInstallBanner" src/App.tsx && grep -q "OfflineBanner" src/App.tsx && npm run build && echo OK</automated>
  </verify>
  <done>Offline shell and iOS install education render conditionally on every route (PWA-03; SUMMARY.md Spec Gap #3).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify installability, single-SW registration, and offline shell on desktop Chrome</name>
  <what-built>The completed PWA: unified SW (Workbox + FCM slot + Storage exclusion), offline shell, and iOS install banner. Installability rests on the valid manifest (plan 01-01) plus the now-registered SW.</what-built>
  <how-to-verify>
    1. Run `npm run build` then `npm run preview` (serves the production build over HTTP, which is sufficient for localhost SW registration in Chrome).
    2. Open Chrome DevTools → Application → Service Workers: confirm EXACTLY ONE service worker is registered and activated, and the page does NOT reload in a loop (PWA-04, Success Criterion 5).
    3. Application → Manifest: confirm name, icons (192 + 512), theme_color, display "standalone", start_url are present and there are no manifest errors; confirm the install icon appears in the Chrome omnibox and the app installs (PWA-01).
    4. DevTools → Network → set "Offline", then reload: confirm the app renders the "You're offline" shell (app chrome visible), NOT a blank white page (PWA-03).
    5. Toggle Network back to "Online": confirm the offline banner clears.
  </how-to-verify>
  <resume-signal>Type "approved" if exactly one SW registers with no reload loop, the app installs, and the offline shell renders; otherwise describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → service worker | SW mediates fetches and caches app-shell assets |
| SW scope → FCM | Background message handler runs in SW context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | Rogue/second service worker hijacking scope | mitigate | Single injectManifest SW scoped to own origin; explicit check that no firebase-messaging-sw.js exists (Task 1); DevTools confirms exactly one SW (Task 3) |
| T-01-08 | Denial of Service | SW intercepting Storage uploads stalls the app | mitigate | firebasestorage.googleapis.com fetch exclusion preserved from plan 01-01 (PWA-05) |
| T-01-09 | Information Disclosure | Firebase web config literals in SW bundle | accept | Web config is public by design; no Admin credentials in SW; rules + App Check (later phases) are the real control |
</threat_model>

<verification>
- `npm run build` compiles the unified SW and app; SW emitted to dist/.
- No second service worker file exists.
- Human-verify checkpoint confirms single-SW registration, installability, and offline shell.
</verification>

<success_criteria>
- PWA-01: app installable on desktop Chrome (valid manifest + registered SW).
- PWA-03: offline shell renders "You're offline" gracefully.
- PWA-04: one unified SW handles Workbox precache + FCM background messages; no reload loop.
- iOS install-education banner shows only when `isIOS && !isStandalone`.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done.
</output>
